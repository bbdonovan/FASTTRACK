To run:

At cmd prompt, do the following:
1. set GEMINI_API_KEY=your_gemini_api_key (go to aistudio.google.com to get a free api key)
2. pip install Flask requests
3. python app.py
4. open Chrome and go to local url displayed in the command console (usually http://127.0.0.1:5000)
